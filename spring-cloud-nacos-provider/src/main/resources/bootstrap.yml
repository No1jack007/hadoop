host: 127.0.0.1
spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      config:
        server-addr: ${host}:8848
        file-extension: yaml
        prefix: nacos-provider
  profiles:
    active: dev